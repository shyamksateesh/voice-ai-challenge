# Python cache
__pycache__/
*.pyc

# Jupyter Notebook cache
.ipynb_checkpoints/
*.ipynb

# Media files (inputs/outputs)
*.mp4
*.mp3
*.mov
*.avi
*.m4a
*.flac
*.wav

# --- Project Folders ---
# Ignore the *contents* of these folders
uploads/*
outputs/*
temp_demucs_output/

# --- But DO NOT ignore the folders themselves ---
# We want the folders, just not their contents.
# (This is a bit advanced, you can also just add .gitkeep files)
!uploads/
!outputs/
!temp_demucs_output/